import {NavLink,Outlet,useNavigate} from 'react-router-dom';import {useAuth} from '../store/auth';import {Button} from './ui/button';
export default function Layout(){const {user,logout}=useAuth();const nav=useNavigate();const links=[['/dashboard','Dashboard'],['/links','Links'],['/analytics','Analytics'],['/bio','Bio page']];return <div className="min-h-screen bg-zinc-50"><header className="sticky top-0 z-20 border-b bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3"><button onClick={()=>nav('/dashboard')} className="text-lg font-bold tracking-tight">Shortly</button><nav className="hidden gap-1 md:flex">{links.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`rounded-md px-3 py-2 text-sm ${isActive?'bg-zinc-100 font-medium':'text-zinc-600 hover:bg-zinc-100'}`}>{label}</NavLink>)}</nav><div className="flex items-center gap-2"><span className="hidden text-sm text-zinc-500 sm:inline">@{user?.username}</span><Button variant="outline" size="sm" onClick={async()=>{await logout();nav('/login')}}>Logout</Button></div></div></header><main className="mx-auto max-w-7xl px-4 py-6"><Outlet/></main></div>}
