{
  "name": "branded-shortlink-hub",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "setup": "bash setup.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
